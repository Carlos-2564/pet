基础功能和之前一致，但优化了「透明背景处理」和「配置保存」，确保动图没有黑边，设置不会丢失：

1. 画布与右键菜单（交互入口）
画布是显示动图的载体，右键菜单是功能总入口：
2. 控制栏（快捷操作按钮）
控制栏包含常用按钮，默认隐藏，通过右键菜单调出，方便快速切换动图和控制播放：
3. 拖拽与缩放（自由调整位置和大小）
鼠标按住可拖动桌宠，滚轮可放大缩小，还能固定窗口防止误操作：
4. 动图加载与播放（核心功能）
加载gif文件夹中的动图，处理透明背景避免黑边，支持 GIF 动画循环播放和暂停：
5. 动图切换与自动播放
支持手动切换上一张 / 下一张 / 随机动图，也能设置自动循环播放，间隔可自定义：
6. 配置保存与透明度调节
自动保存窗口大小、透明度、播放间隔等设置，下次打开直接恢复：