打开 PyCharm（或 VS Code，新手推荐 PyCharm 社区版，免费好用），新建 Python 文件，先写核心框架代码 —— 主要处理路径、窗口样式和变量初始化，为后续功能打基础。

1. 导入必备库​
2. 定义桌宠类与初始化函数（核心）
用面向对象的方式写代码，重点处理 3 件事：打包环境适配、哈希验证初始化、窗口基础样式
3. 运行基础框架
此时点击 PyCharm 的「Run」按钮，会弹出一个无边框的透明窗口（暂时没有动图，属于正常现象），说明基础框架没问题，接下来加核心功能。